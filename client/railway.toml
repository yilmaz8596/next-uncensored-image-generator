[build]
builder = "DOCKERFILE"
dockerfilePath = "/Dockerfile"
watchPatterns = ["client/**", "Dockerfile"]

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
